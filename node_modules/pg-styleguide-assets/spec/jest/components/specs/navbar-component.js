jest.autoMockOff();
require(path(pathMap.components, "navbar/navbar"));

describe("nav bar", function() {
  var rendered;
  var LeftComponent = genMockComponent();
  var RightComponent = genMockComponent();

  beforeEach(function() {
    rendered = utils.renderIntoDocument(
      <PG.Components.NavBar >
        <LeftComponent />
        <RightComponent />
      </PG.Components.NavBar>
    );
  });

  it("adds first child to bar", function() {
    expect(rendered.props.children[0]).toBeTruthy();
  });

  it("adds second child to bar", function() {
    expect(rendered.props.children[1]).toBeTruthy();
  });
});